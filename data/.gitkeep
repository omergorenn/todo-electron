# This file ensures that the data directory is tracked by Git
# while the actual data files are ignored (as specified in .gitignore)
# The directory structure is preserved, but user data is not committed. 